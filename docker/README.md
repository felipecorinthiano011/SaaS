# Docker Orchestration

Use Docker Compose for local full-stack development.

```powershell
cd docker
docker compose up --build
```

This starts:

- Postgres on `5432`
- AI service on `8000`
- Backend API on `8080`
- Frontend on `4200`

